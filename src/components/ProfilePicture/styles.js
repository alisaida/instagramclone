import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    margin: 5,
    borderRadius: 50,
    borderWidth: 3,
    borderColor: "#a61985"
  },
  image: {
    borderRadius: 50,
    borderWidth: 2.5,
    borderColor: "#ffffff"
  }
})

export default styles;
