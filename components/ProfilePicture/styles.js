import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    margin: 5,
    borderRadius: 40,
    borderWidth: 3,
    borderColor: "#a61985"
  },
  image: {
    borderRadius: 40,
    borderWidth: 1.5,
    borderColor: "#ffffff"
  }
})

export default styles;
